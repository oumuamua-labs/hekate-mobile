[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false
description = "Hekate mobile build orchestrator (doctor + ios-build + android-build)."

[dependencies]
clap = { version = "4", features = ["derive"] }
toml = "1.1.2+spec-1.1.0"